galaxy_info:
  author: Derek Merck
  description: Configure and run a [Redis](https://redis.io) kv-store in a Docker container.

  role_name: redis-docker

  license: MIT

  min_ansible_version: 2.4

  galaxy_tags:
  - docker
  - redis

dependencies:
  - role: geerlingguy.pip
    pip_install_packages: docker
  - role: geerlingguy.docker